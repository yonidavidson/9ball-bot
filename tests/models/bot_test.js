var bot = require('../../models/bot');
var assert = require('chai').assert;

describe('bot validate', function(){
  it('should return: \"err....\"', function(){
    assert.notEqual(true,true);
  });
});